# Template for local secrets. Copy this to a file named ".env" and fill in your
# real values (from command-center/push-config.json). ".env" is gitignored and
# must NEVER be committed or baked into the image.
TELEGRAM_BOT_TOKEN=123456:your-bot-token-here
TELEGRAM_CHAT_ID=123456789
GRACE_SECONDS=20
